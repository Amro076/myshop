<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootswatch/5.2.1/flatly/bootstrap.min.css" integrity="sha512-wIYoddftNw7JnaeYbKG4yL4CGGBYBhmcFEw8qobAu8zsDmEW/ZxfeWXyNbiUZ4BE5me64k2JuJRP9r2/i0Ty1Q==" crossorigin="anonymous" referrerpolicy="no-referrer" />
        <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.3.1/css/all.css" integrity="sha384-mzrmE5qonljUremFsqc01SB46JvROS7bZs3IO2EmfFsd15uHvIt+Y8vEf7N7fWAU" crossorigin="anonymous">
        <link rel="stylesheet" href="{{asset('style/style.css') }}">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
       
    </head>
    <body>
        <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
            <div class="container-fluid">
                <a class="navbar-brand" href="{{ path('app_main') }}"><small>MY</small>SHOP</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarColor01">
                    <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link active" href="{{ path('product_home')}}">Nos produits
                        <span class="visually-hidden">(current)</span>
                        </a>
                    </li>
                    {# <li class="nav-item">
                        <a class="nav-link" href="{{ path('product_new') }}">formulaire</a>
                    </li> #}
                    {% if app.user %}
                     <li class="nav-item">
                        <a class="nav-link" href="{{ path('profil') }}">Mon profil</a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_card') }}">Mon panier</a>
                    </li>
                    {% endif %}
                    
                 
            </ul>
            
            
            <form class="d-flex">
            {% if app.user %}
                <a class="btn btn-primary" href="{{ path('app_logout')}}">Déconnexion</a>
               
            {% else %}
                <a class="btn btn-primary" href="{{ path('app_login')}}">Connectez-vous</a>
                <a class="btn btn-primary" href="{{ path('app_register') }}">Inscription</a>
            {% endif %}
            </form>
            </div>
        </div>
        

        {% if is_granted('ROLE_ADMIN') %}  
        <a href="{{ path('admin') }}" class="nav-link btn btn-secondary m-2 my-2 my-sm-0" >BACKOFFICE</a>
        {% endif %}
</nav>
        {% block body %}{% endblock %}
        <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.2.2/js/bootstrap.bundle.min.js" integrity="sha512-BOsvKbLb0dB1IVplOL9ptU1EYA+LuCKEluZWRUYG73hxqNBU85JBIBhPGwhQl7O633KtkjMv8lvxZcWP+N3V3w==" crossorigin="anonymous" referrerpolicy="no-referrer"></script>
    </body>
</html>
