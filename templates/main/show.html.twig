{% extends "base.html.twig" %}

{% block body %}


				<article class="col-md-5 mx-auto text-center border border-dark mb-3 p-3 mt-3 mr-1">
					<h3 class="card-header">{{ product.titre }}</h3>
					<img src="{{ product.photo }}" class="w-100" alt="Image du véhicule">
					<div class="card-body">
						<p class="card-text">{{ product.description }}</p>
						<p class="card-text text-end fw-bold">{{ product.couleur }}</p>
                        <p class="card-text text-end fw-bold">{{ product.collection }}</p>
                        <p class="card-text text-end fw-bold">{{ product.prix }}€</p>
						<div class="form-group">
						<label for="exampleSelect1" class="form-label mt-4">Il nous reste {{ product.stock }} Tee-shirt en stock</label>
						
						</select>
						</div>
                        
					</div>
							
							
						
                        <a href="{{ path('card_add', {id: product.id}) }}" class="card-link btn btn-primary">Ajouter au panier</a>
                        
				
							
					
					
					
					
				</article>


{% endblock %}